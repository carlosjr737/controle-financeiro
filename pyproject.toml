[project]
name = "controle-financeiro"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["SQLAlchemy>=2.0", "openpyxl>=3.1"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
